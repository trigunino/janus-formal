import JanusFormal.Branches.FundamentalGeometryPVariationalPrinciple.Gates.P0EFTJanusMappingTorusCanonicalLatitudeDeckCauchyDataLinear4D
import JanusFormal.Branches.FundamentalGeometryPVariationalPrinciple.Gates.P0EFTJanusMappingTorusCanonicalPhysicalScalarFirstSheetBoundaryExtension4D

/-!
# Smooth Cauchy extension from the global candidate

Choose linear value and normal representatives on the latitude base, periodic and
antiperiodic respectively.  Their exact Cauchy-jet candidate is already a global
linear scalar function on the mapping torus.  If this extension-by-zero
candidate is smooth, it bundles into a linear smooth quotient-field extension.
The exact value and normal `L²` traces then follow from the jet theorem.

Thus the remaining global extension theorem is reduced to one clean statement:
smoothness of the explicitly constructed candidate.
-/

namespace JanusFormal
namespace P0EFTJanusMappingTorusCanonicalPhysicalScalarCauchyJetCandidateExtension4D

set_option autoImplicit false
noncomputable section

open scoped Manifold ContDiff ENNReal
open MeasureTheory Set Topology Filter
open P0EFTJanusMappingTorusQuotient
open P0EFTJanusMappingTorusSmoothAtlasFrontier
open P0EFTJanusMappingTorusSmoothQuotientManifold
open P0EFTJanusMappingTorusSmoothFieldDescent4D
open P0EFTJanusMappingTorusCanonicalPhysicalH1TraceBound4D
open P0EFTJanusMappingTorusCanonicalPhysicalScalarFirstSheetHilbertTrace4D
open P0EFTJanusMappingTorusCanonicalPhysicalScalarFirstSheetBoundaryExtension4D
open P0EFTJanusMappingTorusCanonicalLatitudeCauchyJetDeckGluing4D
open P0EFTJanusMappingTorusCanonicalLatitudeCauchyJetCollarQuotient4D
open P0EFTJanusMappingTorusCanonicalPhysicalScalarCauchyJetGlobalCandidate4D
open P0EFTJanusMappingTorusCanonicalLatitudeDeckCauchyDataLinear4D
open P0EFTJanusMappingTorusScalarBoundarySmoothExtensionDensity4D

universe x y

variable (period : Real) (hPeriod : period ≠ 0)

private abbrev sphereData := reflectedSphereData period hPeriod
private abbrev EffectiveQuotient := MappingTorus (sphereData period hPeriod)
private abbrev BoundaryL2 := CanonicalPhysicalScalarFirstSheetL2 period

local instance effectiveQuotientChartedSpace :
    ChartedSpace CoverModel (EffectiveQuotient period hPeriod) :=
  reflectedSphereQuotientChartedSpace period hPeriod

local instance effectiveQuotientIsManifold :
    IsManifold coverModelWithCorners ω (EffectiveQuotient period hPeriod) :=
  reflectedSphereQuotient_isManifold period hPeriod

/-- Deck Cauchy data generated by separate value and normal representatives. -/
def canonicalLatitudeDeckCauchyDataOfRepresentatives
    {ValueCore : Type x} {NormalCore : Type y}
    [AddCommGroup ValueCore] [Module Real ValueCore]
    [AddCommGroup NormalCore] [Module Real NormalCore]
    (valueRepresentative : ValueCore →ₗ[Real]
      (CanonicalLatitudeBase → Real))
    (normalRepresentative : NormalCore →ₗ[Real]
      (CanonicalLatitudeBase → Real))
    (valuePeriodic : ∀ value : ValueCore,
      CanonicalLatitudeValueDeckPeriodic period (valueRepresentative value))
    (normalAntiperiodic : ∀ normal : NormalCore,
      CanonicalLatitudeNormalDeckAntiperiodic period
        (normalRepresentative normal))
    (data : ValueCore × NormalCore) :
    CanonicalLatitudeDeckCauchyData period where
  value := valueRepresentative data.1
  normal := normalRepresentative data.2
  value_periodic := valuePeriodic data.1
  normal_antiperiodic := normalAntiperiodic data.2

/-- The preceding construction is linear in the boundary-core pair. -/
def canonicalLatitudeDeckCauchyDataLinearMapOfRepresentatives
    {ValueCore : Type x} {NormalCore : Type y}
    [AddCommGroup ValueCore] [Module Real ValueCore]
    [AddCommGroup NormalCore] [Module Real NormalCore]
    (valueRepresentative : ValueCore →ₗ[Real]
      (CanonicalLatitudeBase → Real))
    (normalRepresentative : NormalCore →ₗ[Real]
      (CanonicalLatitudeBase → Real))
    (valuePeriodic : ∀ value : ValueCore,
      CanonicalLatitudeValueDeckPeriodic period (valueRepresentative value))
    (normalAntiperiodic : ∀ normal : NormalCore,
      CanonicalLatitudeNormalDeckAntiperiodic period
        (normalRepresentative normal)) :
    ValueCore × NormalCore →ₗ[Real]
      CanonicalLatitudeDeckCauchyData period where
  toFun := canonicalLatitudeDeckCauchyDataOfRepresentatives period
    valueRepresentative normalRepresentative valuePeriodic normalAntiperiodic
  map_add' first second := by
    ext <;> simp [canonicalLatitudeDeckCauchyDataOfRepresentatives]
  map_smul' scalar data := by
    ext <;> simp [canonicalLatitudeDeckCauchyDataOfRepresentatives]

/-- Boundary-core representatives and smoothness of their explicit global
candidate. -/
structure CanonicalPhysicalScalarCauchyJetCandidateExtensionData
    (ValueCore : Type x) (NormalCore : Type y)
    [AddCommGroup ValueCore] [Module Real ValueCore]
    [AddCommGroup NormalCore] [Module Real NormalCore] where
  valueRepresentative : ValueCore →ₗ[Real]
    (CanonicalLatitudeBase → Real)
  normalRepresentative : NormalCore →ₗ[Real]
    (CanonicalLatitudeBase → Real)
  valuePeriodic : ∀ value : ValueCore,
    CanonicalLatitudeValueDeckPeriodic period (valueRepresentative value)
  normalAntiperiodic : ∀ normal : NormalCore,
    CanonicalLatitudeNormalDeckAntiperiodic period
      (normalRepresentative normal)
  valueEmbedding : ValueCore →ₗ[Real] BoundaryL2 period
  normalEmbedding : NormalCore →ₗ[Real] BoundaryL2 period
  valueDense : DenseRange valueEmbedding
  normalDense : DenseRange normalEmbedding
  valueEmbedding_ae : ∀ value : ValueCore,
    (valueEmbedding value : CanonicalLatitudeBase → Real) =ᵐ[
      canonicalLatitudeBaseMeasure period]
      valueRepresentative value
  normalEmbedding_ae : ∀ normal : NormalCore,
    (normalEmbedding normal : CanonicalLatitudeBase → Real) =ᵐ[
      canonicalLatitudeBaseMeasure period]
      normalRepresentative normal
  candidate_contMDiff : ∀ data : ValueCore × NormalCore,
    ContMDiff coverModelWithCorners 𝓘(Real, Real) ∞
      (canonicalLatitudeCauchyJetGlobalCandidate period hPeriod
        (canonicalLatitudeDeckCauchyDataOfRepresentatives period
          valueRepresentative normalRepresentative valuePeriodic
            normalAntiperiodic data))

namespace CanonicalPhysicalScalarCauchyJetCandidateExtensionData

/-- Linear deck-data map associated to the candidate extension. -/
def deckData
    {ValueCore : Type x} {NormalCore : Type y}
    [AddCommGroup ValueCore] [Module Real ValueCore]
    [AddCommGroup NormalCore] [Module Real NormalCore]
    (candidate : CanonicalPhysicalScalarCauchyJetCandidateExtensionData
      period hPeriod ValueCore NormalCore) :
    ValueCore × NormalCore →ₗ[Real]
      CanonicalLatitudeDeckCauchyData period :=
  canonicalLatitudeDeckCauchyDataLinearMapOfRepresentatives period
    candidate.valueRepresentative candidate.normalRepresentative
    candidate.valuePeriodic candidate.normalAntiperiodic

/-- Smooth global physical Cauchy extension. -/
def extension
    {ValueCore : Type x} {NormalCore : Type y}
    [AddCommGroup ValueCore] [Module Real ValueCore]
    [AddCommGroup NormalCore] [Module Real NormalCore]
    (candidate : CanonicalPhysicalScalarCauchyJetCandidateExtensionData
      period hPeriod ValueCore NormalCore) :
    ValueCore × NormalCore →ₗ[Real]
      SmoothQuotientField period hPeriod Real where
  toFun data :=
    { toFun := canonicalLatitudeCauchyJetGlobalCandidate period hPeriod
        (candidate.deckData data)
      contMDiff_toFun := candidate.candidate_contMDiff data }
  map_add' first second := by
    apply SmoothQuotientField.ext period hPeriod Real
    intro point
    have hCandidate := CanonicalLatitudeDeckCauchyData.globalCandidate_add
      period hPeriod (candidate.deckData first) (candidate.deckData second)
    have hDeck : candidate.deckData (first + second) =
        candidate.deckData first + candidate.deckData second :=
      map_add candidate.deckData first second
    rw [hDeck]
    exact congrFun hCandidate point
  map_smul' scalar data := by
    apply SmoothQuotientField.ext period hPeriod Real
    intro point
    have hCandidate := CanonicalLatitudeDeckCauchyData.globalCandidate_smul
      period hPeriod scalar (candidate.deckData data)
    have hDeck : candidate.deckData (scalar • data) =
        scalar • candidate.deckData data :=
      map_smul candidate.deckData scalar data
    rw [hDeck]
    exact congrFun hCandidate point

/-- Exact value trace of the constructed smooth extension. -/
theorem valueTrace_extension
    {ValueCore : Type x} {NormalCore : Type y}
    [AddCommGroup ValueCore] [Module Real ValueCore]
    [AddCommGroup NormalCore] [Module Real NormalCore]
    (candidate : CanonicalPhysicalScalarCauchyJetCandidateExtensionData
      period hPeriod ValueCore NormalCore)
    (data : ValueCore × NormalCore) :
    smoothCanonicalPhysicalScalarFirstSheetValueL2 period hPeriod
        (candidate.extension data) =
      candidate.valueEmbedding data.1 := by
  apply Lp.ext
  filter_upwards
    [smoothCanonicalPhysicalScalarFirstSheetValueL2_ae period hPeriod
      (candidate.extension data),
     candidate.valueEmbedding_ae data.1]
    with base hTrace hEmbedding
  rw [hTrace, hEmbedding]
  change canonicalLatitudeCauchyJetGlobalCandidate period hPeriod
      (candidate.deckData data)
      (quotientNormalLatitude period hPeriod
        (canonicalLatitudeAnchor period hPeriod base) 0) =
    candidate.valueRepresentative data.1 base
  exact canonicalLatitudeCauchyJetGlobalCandidate_value
    period hPeriod (candidate.deckData data) base

/-- Exact normal trace of the constructed smooth extension. -/
theorem normalTrace_extension
    {ValueCore : Type x} {NormalCore : Type y}
    [AddCommGroup ValueCore] [Module Real ValueCore]
    [AddCommGroup NormalCore] [Module Real NormalCore]
    (candidate : CanonicalPhysicalScalarCauchyJetCandidateExtensionData
      period hPeriod ValueCore NormalCore)
    (data : ValueCore × NormalCore) :
    smoothCanonicalPhysicalScalarFirstSheetNormalL2 period hPeriod
        (candidate.extension data) =
      candidate.normalEmbedding data.2 := by
  apply Lp.ext
  filter_upwards
    [smoothCanonicalPhysicalScalarFirstSheetNormalL2_ae period hPeriod
      (candidate.extension data),
     candidate.normalEmbedding_ae data.2]
    with base hTrace hEmbedding
  rw [hTrace, hEmbedding]
  change deriv (fun normal =>
      canonicalLatitudeCauchyJetGlobalCandidate period hPeriod
        (candidate.deckData data)
        (quotientNormalLatitude period hPeriod
          (canonicalLatitudeAnchor period hPeriod base) normal)) 0 =
    candidate.normalRepresentative data.2 base
  exact canonicalLatitudeCauchyJetGlobalCandidate_normalDerivative
    period hPeriod (candidate.deckData data) base

/-- Exact paired Cauchy trace. -/
theorem cauchyTrace_extension
    {ValueCore : Type x} {NormalCore : Type y}
    [AddCommGroup ValueCore] [Module Real ValueCore]
    [AddCommGroup NormalCore] [Module Real NormalCore]
    (candidate : CanonicalPhysicalScalarCauchyJetCandidateExtensionData
      period hPeriod ValueCore NormalCore)
    (data : ValueCore × NormalCore) :
    smoothCanonicalPhysicalScalarFirstSheetCauchyTrace period hPeriod
        (candidate.extension data) =
      canonicalScalarBoundaryCorePairEmbedding
        candidate.valueEmbedding candidate.normalEmbedding data := by
  ext
  · exact candidate.valueTrace_extension data
  · exact candidate.normalTrace_extension data

/-- Dense range of the smooth Cauchy trace. -/
theorem boundaryTrace_denseRange
    {ValueCore : Type x} {NormalCore : Type y}
    [AddCommGroup ValueCore] [Module Real ValueCore]
    [AddCommGroup NormalCore] [Module Real NormalCore]
    (candidate : CanonicalPhysicalScalarCauchyJetCandidateExtensionData
      period hPeriod ValueCore NormalCore) :
    DenseRange
      (smoothCanonicalPhysicalScalarFirstSheetCauchyTrace period hPeriod) := by
  let extensionData : CanonicalScalarSmoothCauchyExtensionData
      (ValueCore := ValueCore) (NormalCore := NormalCore)
      (smoothCanonicalPhysicalScalarFirstSheetCauchyTrace period hPeriod) :=
    { valueEmbedding := candidate.valueEmbedding
      normalEmbedding := candidate.normalEmbedding
      valueDense := candidate.valueDense
      normalDense := candidate.normalDense
      extension := candidate.extension
      boundary_extension := candidate.cauchyTrace_extension }
  exact extensionData.boundaryTrace_denseRange

/-- Conversion to the complete smooth physical Cauchy-extension data. -/
def toSmoothCauchyExtensionData
    {ValueCore : Type x} {NormalCore : Type y}
    [AddCommGroup ValueCore] [Module Real ValueCore]
    [AddCommGroup NormalCore] [Module Real NormalCore]
    (candidate : CanonicalPhysicalScalarCauchyJetCandidateExtensionData
      period hPeriod ValueCore NormalCore)
    (operatorData :
      P0EFTJanusMappingTorusCanonicalPhysicalScalarEulerAtlas4D.CanonicalPhysicalScalarEulerGlobalOperatorData
        period hPeriod massSquared)
    (bulkGreenStokes : ∀ first second :
      SmoothQuotientField period hPeriod Real,
      inner Real (operatorData.toBulkL2LinearMap first)
            (P0EFTJanusMappingTorusCanonicalPhysicalBulkL2H1Bridge4D.smoothToCanonicalPhysicalBulkL2
              period hPeriod second) -
          inner Real
            (P0EFTJanusMappingTorusCanonicalPhysicalBulkL2H1Bridge4D.smoothToCanonicalPhysicalBulkL2
              period hPeriod first)
            (operatorData.toBulkL2LinearMap second) =
        P0EFTJanusMappingTorusCutBulkGlobalOrientedBoundaryCurrent4D.cutBulkGlobalOrientedScalarCurrentIntegral
          period hPeriod first second) :
    CanonicalPhysicalScalarSmoothCauchyExtensionData
      period hPeriod massSquared ValueCore NormalCore where
  operatorData := operatorData
  valueEmbedding := candidate.valueEmbedding
  normalEmbedding := candidate.normalEmbedding
  valueDense := candidate.valueDense
  normalDense := candidate.normalDense
  extension := candidate.extension
  boundary_extension := candidate.cauchyTrace_extension
  bulk_green_stokes := bulkGreenStokes

/-- Candidate-extension certificate. -/
theorem certificate
    {ValueCore : Type x} {NormalCore : Type y}
    [AddCommGroup ValueCore] [Module Real ValueCore]
    [AddCommGroup NormalCore] [Module Real NormalCore]
    (candidate : CanonicalPhysicalScalarCauchyJetCandidateExtensionData
      period hPeriod ValueCore NormalCore) :
    DenseRange
        (smoothCanonicalPhysicalScalarFirstSheetCauchyTrace period hPeriod) ∧
      (∀ data,
        smoothCanonicalPhysicalScalarFirstSheetCauchyTrace period hPeriod
            (candidate.extension data) =
          canonicalScalarBoundaryCorePairEmbedding
            candidate.valueEmbedding candidate.normalEmbedding data) :=
  ⟨candidate.boundaryTrace_denseRange,
    candidate.cauchyTrace_extension⟩

end CanonicalPhysicalScalarCauchyJetCandidateExtensionData

end
end P0EFTJanusMappingTorusCanonicalPhysicalScalarCauchyJetCandidateExtension4D
end JanusFormal
