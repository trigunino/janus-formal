name: Program D fundamental geometry

on:
  push:
    branches:
      - research/fundamental-geometry-d
    paths:
      - "JanusFormal/Branches/FundamentalGeometryD.lean"
      - "JanusFormal/Branches/FundamentalGeometryD/**"
      - "JanusFormal/Branches/JanusTwistedHopfGeometry.lean"
      - "JanusFormal/Branches/JanusTwistedHopfGeometry/**"
      - "scripts/audit_janus_fundamental_geometry_d.py"
      - "tests/test_janus_fundamental_geometry_d.py"
      - "lean-toolchain"
      - "lakefile.lean"
      - "lake-manifest.json"
      - ".github/workflows/fundamental-geometry-d.yml"
  pull_request:
    paths:
      - "JanusFormal/Branches/FundamentalGeometryD.lean"
      - "JanusFormal/Branches/FundamentalGeometryD/**"
      - "JanusFormal/Branches/JanusTwistedHopfGeometry.lean"
      - "JanusFormal/Branches/JanusTwistedHopfGeometry/**"
      - "scripts/audit_janus_fundamental_geometry_d.py"
      - "tests/test_janus_fundamental_geometry_d.py"
      - "lean-toolchain"
      - "lakefile.lean"
      - "lake-manifest.json"
  workflow_dispatch:

jobs:
  lean:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Lean and Mathlib cache
        uses: leanprover/lean-action@v1
        with:
          auto-config: false
          build: false
          test: false
          lint: false
          use-mathlib-cache: true
      - name: Build Program D
        run: |
          set +e
          lake build JanusFormal.Branches.FundamentalGeometryD > program-d-lean.log 2>&1
          status=$?
          cat program-d-lean.log
          exit $status
      - name: Upload Lean log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: program-d-lean-log
          path: program-d-lean.log
          if-no-files-found: error

  python-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e . pytest
      - run: python scripts/audit_janus_fundamental_geometry_d.py
      - name: Run focused tests
        env:
          PYTHONPATH: .
        run: python -m pytest -q tests/test_janus_fundamental_geometry_d.py
