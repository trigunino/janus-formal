# HESSIAN-GLOBAL-01 — concrete product-throat long-time terminal

## Strongest current facade

```text
JanusFormal.Branches.FundamentalGeometryPVariationalPrinciple.Gates.
  P0EFTJanusProgramPGlobalHessianPreferredFiveSectorH14D11SelectedTraceShortDominatedProductThroatLongTimeNuclearDuhamelZetaTerminal4D
```

This facade keeps the preferred Candidate-A route from the genuine H14 reduced
Green operator through the D11 unitary frame and the physical five-sector
spectral-cut atlas.  Its new content is that the complete long-time analysis of
each reference chart comes from an explicit circle times monopole-sphere
spectrum.

## 1. Concrete positive heat gap

For a product-throat spectral datum with sphere radius `R`, the sphere tower
satisfies

```text
1 / R^2 <= sphereEigenvalueSquared(level).
```

The squared circle eigenvalue is nonnegative.  Therefore every complete
product mode has energy at least

```text
c_product = 1 / R^2 > 0.
```

This is a positive spectral statement for the reference heat generator.  It is
not inferred from the H14 Fredholm estimate

```text
c * norm(x) <= norm(H x),
```

which alone would allow negative spectrum and hence would not imply heat decay.

## 2. Infinite product heat trace

The existing product Hilbert space retains

* every monopole-sphere level;
* its exact finite degeneracy;
* every circle Fourier mode.

For positive time the diagonal Gaussian weights are summable and define the
genuine nuclear product trace.  Comparing time `t >= 1` to unit time gives

```text
Tr K_product(t)
  <= exp(c_product) * Tr K_product(1) * exp(-c_product * t).
```

Thus the scale is generated by the actual unit-time nuclear trace:

```text
C_product = exp(c_product) * Tr K_product(1).
```

The corresponding envelope is integrable on every half-line contained in
`(1, +infinity)`.

## 3. Duhamel insertion domination

For a differentiable reference family, the remaining natural operator estimate
has the form

```text
abs(Tr D_a(t)) <= B_a * Tr K_product(t)
```

locally uniformly in the family parameter.  The product heat estimate then
generates

```text
abs(Tr D_a(t))
  <= B_a * C_product * exp(-c_product * t).
```

This automatically supplies the positive rate, the scale, integrability of the
majorant and differentiation under the weighted long-time heat integral.

## 4. Terminal primitive decay

The finite-cutoff long-time identity contains a terminal primitive.  Its
concrete remaining estimate is

```text
norm(terminalPrimitive_a(T))
  <= C_a * Tr K_product(T).
```

The same product gap yields

```text
norm(terminalPrimitive_a(T))
  <= C_a * C_product * exp(-c_product * T),
```

so the primitive tends to zero.  The long-time integrated operator is therefore
the matching operator, without an independently supplied vector-valued limit
or decay rate.

## 5. Selected trace and zeta coefficient

The long-time product construction is inserted into the existing selected-trace
boundary packet.  The short-time renormalized target remains definitionally

```text
G_ref,a H'_ref,a,
```

and its intrinsic nuclear certificate remains the one already selected by the
reference chart.  Hence

```text
finitePartLogDerivative(a) = Tr(G_ref,a H'_ref,a),
T_reference(a) = -Tr(G_ref,a H'_ref,a).
```

The D11 transported actual coefficient is zero, so

```text
T_relative(a) = Tr(G_ref,a H'_ref,a).
```

Canonical Mellin Schwarz reflection still derives the reality of the regularized
zeta derivative.

## 6. Candidate-A atlas

The base reference and every local spectral-cut reference now carry their own
`ProductThroatSpectralData` and circle twist.  The atlas adapter generates the
former short-dominated/long-exponential frontend and preserves

* the operator-generated reference cocycle;
* the selected intrinsic logarithmic traces;
* the relative zeta coefficients;
* the Quillen determinant charts;
* the resolved and differentiable physical kernel family.

No arbitrary long-time rate survives at the atlas boundary.

## 7. Remaining irreducible work

For the genuine Candidate-A base reference and each local spectral-cut
reference, the remaining proofs are now concrete:

1. identify the exact product spectral datum: sphere radius, circle period,
   monopole charge and twist;
2. identify the reference heat operator with the already constructed product
   spectral heat operator;
3. prove the insertion estimate
   `abs(Tr D_a(t)) <= B_a * Tr K_product(t)`;
4. prove the terminal-primitive estimate
   `norm(terminalPrimitive_a(T)) <= C_a * Tr K_product(T)`;
5. prove the remaining rank-one spectral sum/integral exchange;
6. complete the short-time heat asymptotic subtraction and counterterm
   convergence;
7. establish the selected Mellin continuation domain and analytic regularity.

The positive long-time gap, exponential envelope, its integrability and the
terminal decay theorem are no longer independent premises.

## Validation scope

The dedicated workflow

```text
.github/workflows/program-p-product-throat-positive-gap.yml
```

builds the concrete product heat-gap theorem, its Duhamel adapters, the
selected-trace boundary and zeta layers, the Candidate-A atlas and the terminal
facade.  This is a targeted validation and not a full repository build.
