# HESSIAN-GLOBAL-01 — fixed-coordinate H14–D11 Green derivative

## Status

The concrete H14 gap and reduced Green are now transported by the same unitary
D11 frame that transports the true kernel and its orthogonal complement.

This note records the additional bridge to the pre-existing differentiable
fixed-Hilbert-space family interface. It is an architectural closure; a final
Lean elaboration/build pass is not claimed here.

## 1. One unitary frame, not two trivializations

The D11 frame supplies

```text
F_a : ambient Hilbert ≃ₗᵢ ambient Hilbert
H_a F_a = F_a H_0.
```

Its restriction gives

```text
T_a : (ker H_0)ᗮ ≃L (ker H_a)ᗮ.
```

`P0EFTJanusProgramPFiniteUnitaryFrameKernelComplementTrivialization4D.lean`
converts this restriction directly into

```text
SelfAdjointKernelComplementFamilyTrivializationData
```

and, using the concrete H14 basepoint gap, into

```text
SelfAdjointKernelComplementUniformGapTrivializationData.
```

The old Bismut–Freed fixed-complement interface and the new D11 frame therefore
refer to the same geometric transport.

## 2. Exact constancy of the reduced operator

The fixed operator is

```text
H_fixed(a) = T_a⁻¹ H_red,a T_a.
```

D11 intertwining proves the literal equality

```text
H_fixed(a) = H_red,0
```

for every parameter. Hence

```text
HasDerivAt H_fixed 0 a.
```

No operator derivative has to be guessed or supplied for this fixed-coordinate
family.

## 3. Exact constancy of the Green

The canonical inverse of `H_fixed(a)` is unique. Since all members of the fixed
operator family equal `H_red,0`, their canonical Greens satisfy

```text
G_fixed(a) = G_fixed(0).
```

The file

```text
P0EFTJanusProgramPGlobalHessianPreferredFiveSectorH14D11FixedCoordinateGreenDerivative4D.lean
```

constructs the existing

```text
DifferentiableSelfAdjointUniformGapFamilyData
GreenDifferentiabilityData
```

with

```text
H'_fixed(a) = 0,
G'_fixed(a) = 0.
```

The standard inverse formula is then generated by the existing generic theory:

```text
G'_fixed(a)
  = -G_fixed(a) H'_fixed(a) G_fixed(a)
  = 0.
```

The H14 inverse-gap estimate is retained:

```text
‖G_fixed(a)‖ ≤ gap⁻¹.
```

## 4. What is closed

The following are no longer independent premises:

1. a second fixed-complement trivialization;
2. differentiability of the fixed reduced operator;
3. differentiability of its canonical Green;
4. the ordinary inverse derivative identity in fixed coordinates;
5. a new family gap or Green bound.

They are consequences of the concrete H14 data and the unitary D11 frame.

## 5. What remains geometric

The nontrivial connection information is not the derivative of the fixed
operator family, which vanishes. It is carried by the moving frame itself.

The next genuine input is operator-topology regularity of

```text
a ↦ F_a
```

and the corresponding connection generator, schematically

```text
A_a = F'_a F_a⁻¹.
```

In ambient moving coordinates one then expects

```text
H'_a = [A_a, H_a],
G'_a = [A_a, G_a] = -G_a H'_a G_a.
```

The selected reference family must be treated with the same frame discipline.
Only after that comparison is in place should the intrinsic trace-class
statement

```text
G_a H'_a - G_ref,a H'_ref,a
```

be connected to the geometric Bismut–Freed one-form and the
higher-dimensional families-index curvature.

## 6. Terminal import

The terminal facade now imports this fixed-coordinate derivative layer:

```text
JanusFormal.Branches.FundamentalGeometryPVariationalPrinciple.Gates.
  P0EFTJanusProgramPGlobalHessianPreferredFiveSectorH14D11UnitaryFredholmGreenTerminal4D
```
