# Janus Current Global Lock

## Status

The current pass closes the alpha/background program as far as the repo can go
without adding a new physical law.

## Closed / Archived

- Z4 legacy CMB work: archived diagnostic, not a closed Janus proof.
- No-fit alpha search: blocked without an explicit boundary state law.
- Radical quantum geometry: future new-theory program, not current Janus closure.
- Alpha superselection sector: formalized and observation-tested.

## Alpha Superselection Result

`alpha` is treated as a global state-sector label, analogous to ADM mass or a
conserved charge.

This is a valid sector theory, but not a no-fit prediction.

## Observation Result

The current SN full-covariance + DESI DR2 BAO runner selects:

```text
q0 -> 0-
```

not an interior Janus sector, with the current paper-native/background-proxy BAO
map.

Classification:

```text
superselection_calibration_closes_negative_for_current_background_proxy
```

## Reopen Conditions

Reopen alpha/background only if one of the following is provided or derived:

1. native Janus BAO/ruler contract;
2. explicit boundary state law fixing `chi_LL`/`alpha`;
3. new paper-native source that fixes the missing scale;
4. observational branch outside background expansion where Janus makes a
   concrete non-background claim.

## Current Recommended Next Work

Do not add more alpha gates.

Either:

- derive a native Janus BAO/ruler contract; or
- move to another Janus observable family: structures, voids/repellers,
  negative lensing, galaxy dynamics, plugstars/compact objects.

## BAO/Ruler Follow-Up

The native BAO contract is now formulated in
`docs/janus_native_bao_ruler_contract.md`.

It adds a stronger obstruction: a published-like interior value `q0=-0.087`
has `z_max ~= 5.747`, so it cannot reach a high-redshift drag epoch on the
same normalized exact branch.

Therefore BAO requires a native early-time/redshift/ruler sector, not only a
different value of `alpha`.
