# Janus Complex Reality Quantum Candidate Workbench

## Purpose

Put the three serious boundary phase-space candidates into the existing quantum
frontier, without promoting any of them to a Janus-derived state law.

## Candidates

| Candidate | Quantum object if accepted | Calculable now | Still missing |
| --- | --- | --- | --- |
| Active throat `S2` | geometric `S2` KKS quantization | topological support | active `Omega_Sigma` pullback and nonzero area component |
| `CP1` spinor/frame orbit | spin-`j` Hilbert space, dimension `2*j/hbar + 1` | period `4*pi*j`, integrality `2*j/hbar in Z` | Janus/PT derivation and map `j -> alpha_m` |
| `aroundSigma x phase` | action-angle torus, integer action sector | `aroundSigma` one-cycle | compact phase, normalized action, alpha map |

## Result

`CP1` is the cleanest quantum candidate. Active throat `S2` is the cleanest
geometric route. `aroundSigma x phase` remains blocked until the second compact
phase is derived.

None currently fixes `alpha`.
