# M18 - Constraints on Janus Cosmological model from recent observations of supernovae type Ia

## Metadata

- Year: 2018
- Axis: expansion
- Source level: published Janus source; equation-level anchor for SN/open-distance formulas
- PDF: `data\raw\janus_library\M18_constraints-on-janus-cosmological-model-from-recent-observations-of-supernovae-t.pdf`
- Text extract: `data\raw\janus_library_text\M18_constraints-on-janus-cosmological-model-from-recent-observations-of-supernovae-t.txt`
- Extraction status: ok

## Logic Of Thought

Use the exact Janus expansion/redshift machinery, then compare against SNe, BAO, DESI and CMB-derived observables without importing Lambda-CDM assumptions silently.

Tags: `expansion`, `signed_mass_sector`, `bimetric_geometry`, `variable_constants`

Curated logic anchors:

- Use the exact Janus expansion solution as the geometry, then fit the SN magnitude-redshift law with a single shape parameter `q0` plus nuisance offset.
- Keep the open-distance mapping explicit; BAO use requires checking that the same ruler assumptions are still valid.

## Formula And Equation Anchors

- `m = 5 log10[z + z^2(1-q0)/(1+q0 z + sqrt(1+2q0z))] + cst` (Eq. 5)
- `q0 = -0.087 +/- 0.015` from the SN fit (Eq. 6)
- `a(u)` proportional to `cosh(u)^2`, with parametric `t(u)` (Eq. 10)
- `q = -1/(2 sinh(u)^2)` and open marker `r = sinh(2(u0-u_e))` (Eqs. 13, 17)

Equation markers found automatically:
- `Eq. (9)`
- `Equation (15)`
- `Eqs. (13)`
- `Eqs. (20)`
- `Eq. (17)`
- `Eq. (14)`
- `Eq. ( 22)`
- `Eqs. (11)`
- `Eq. (20)`

## Core Ideas / Cues

Keyword signals: `negative mass`=19, `Sakharov`=9, `dark matter`=7, `redshift`=7, `dark energy`=5, `negative energy`=4, `variable constants`=4, `supernova`=3.

Short source windows for orientation:
- cordance model · Dark matter · Dark energy · High-z supernovae · Deceleration parameter · Negative mass · Janus Cosmological model · Age of the universe · Bimetric model 1 Introduction Modern c
- matic change which extends General Relativity to a wider model. This very model includes negative mass and neg- ative energy particles whose physical nature can be de- scribed through the dyna

## Observational Hooks

`dark matter`=7, `redshift`=7, `supernova`=3.

Short observable windows:
- eal with a par- ticle embedded in a gravitational ﬁeld where particles follow the same geodesic whatever positive or negative their mass would be. As a conclusion, from Einstein’s equa
- . D’Agostini, J.P. Petit consider that positive and negative masses follow differ- ent geodesic systems, which both derived from both distinct metric tensor ﬁelds g(+) μν and g(−) μν .

## Verification Notes

Short equation/formula windows to check in the PDF:
- the sake of simplicity we will write a ≡ a(+) in the following. A parametric solution of Eq. (9) can be written as: a(u) = α2ch2(u) t(u) = α2 c ( 1 + sh(2u) 2 + u ) (10) with α2 =− 8
- 2q0 − 1 2q0(1 + z) = argsh √ − 1 + 2q0z 2q0(1 + z) (21) Inserting Eqs. (20) and (21) into Eq. (17), after a ‘few’ tech- nical manipulations, using at the end Eq. (14) and consider- in

Manual status: automatic first-pass card. Treat as navigation unless formulas are listed as curated above or verified in the PDF.
