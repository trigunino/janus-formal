# Direct P tensor dispersion and mode-count verdict

Status: isolated audit; no finite mode count imposed.

The current P stack now suffices to combine its actual flat linearized Einstein
symbol with its actual relative interaction Hessian.  With the stabilizing
Lorentzian field-equation sign displayed explicitly, the two TT channels obey

`omega_common^2 = k^2`,

`omega_relative^2 = k^2 + 4 coupling`.

The coefficient `4` belongs to the normalization `G_12 =
(1/2)(omega^2-k^2)h_12`; changing action normalization rescales the named
coupling, not the common/relative conclusion.

The newer shifted Sobolev construction uses the countable lattice `Z^4`.  Its
only Lorentz--Gram kernel is the four-component zero Fourier fiber, and the
physical quotient is represented by the closed zero-free subspace.  The audit
constructs an explicit injective family of nonzero modes indexed by `N`.
Therefore this concrete P realization has infinitely many physical frequency
labels before any ultraviolet cutoff.  It does not yield 1001 states.

This advances the TT sector and mode-count audit.  It does not close the
Candidate-A scalar/vector ADM constraints, because the Sobolev quotient is a
compatibility-operator quotient rather than the quotient of the complete
nonlinear action Hessian.

The tensor audit has also been completed at the polarization level.  Besides
the cross polarization `h_12=h_21`, P's Einstein symbol gives the same wave
polynomial on the independent plus polarization `h_11=-h_22`.  Their linear
independence is proved explicitly.  Hence both TT polarizations obey the same
common massless and relative massive dispersion laws; the result is not an
artifact of one selected matrix component.

An explicit extension audit also proves why this does not automatically close
SVT.  Arbitrary quadratic terms in `h_00^2` or `h_01^2` vanish on both TT
polarizations while changing respectively the lapse-like scalar and shift-like
vector sectors.  Therefore even complete agreement on the TT plane leaves
independent scalar/vector Hessian coefficients undetermined.  The next
non-negotiable input is the full Candidate-A metric Hessian, not more TT
analysis.
