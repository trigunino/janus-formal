# Janus CPT/PT State-Law Route

## Result

The CPT-symmetric-universe literature shows that a global CPT condition can
select a preferred QFT vacuum and can produce particle-sector predictions.

This is directly relevant to Janus because Janus already wants a paired
pre/post or positive/negative sector structure.

## Blocker

The existing Janus assets do not yet compute a PT/CPT-selected vacuum energy,
boundary charge, or bridge mass.

Therefore this route supplies a plausible state principle, but not yet `alpha`.

## Required Next Object

`Janus_PT_vacuum_energy_functional`.

It must define:

- field content on both sheets;
- PT/CPT action on modes;
- vacuum prescription;
- renormalized energy or boundary charge;
- map to `M_bridge/alpha`.
